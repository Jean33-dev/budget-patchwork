
import Expenses from "@/pages/Expenses";

// This is a simple wrapper component to make the imports work correctly
const ExpensesPage = () => {
  return <Expenses />;
};

export default ExpensesPage;
